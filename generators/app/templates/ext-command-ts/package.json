{
    "name": <%- JSON.stringify(name) %>,
    "displayName": <%- JSON.stringify(displayName) %>,
    "description": <%- JSON.stringify(description) %>,
    "version": "0.0.1",
    "publisher": <%- JSON.stringify(publisher) %>,
    "engines": {
        "doppler": <%- JSON.stringify(vsCodeEngine) %>
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onCommand:extension.sayHello"
    ],
    "main": "./out/extension",
    "contributes": {
        "commands": [{
            "command": "extension.sayHello",
            "title": "Hello World"
        }]
    },
    "scripts": {
        "doppler:prepublish": "npm run compile",
        "compile": "tsc -p ./",
        "watch": "tsc -watch -p ./",
        "postinstall": "node ./node_modules/doppler/bin/install",
        "test": "npm run compile && node ./node_modules/doppler/bin/test"
    },
    "devDependencies": {
        "typescript": "^2.6.1",
        "doppler": "^1.1.6",
<%if (tslint) {-%>
        "tslint": "^5.8.0",
<% } -%>
        "@types/node": "^7.0.43",
        "@types/mocha": "^2.2.42"
    }
}